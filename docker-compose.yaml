services:
  client:
    build:
      context: ./client
      dockerfile: Dockerfile.client
    ports:
      - "4200:4200"
    volumes:
      - ./client:/app
      - /app/node_modules
    command: ["ng", "serve", "--host", "0.0.0.0", "--poll", "2000"]
